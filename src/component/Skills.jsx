import React from "react";

const Skills = (props) => {
  // console.log("🚀 ~ Skills ~ props", props);
  return (
    <div>
      <p>Your skills are {props.skills}</p>
    </div>
  );
};

export default Skills;
